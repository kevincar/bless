Installation
============

Install Bless from PyPI:

.. code-block:: bash

   pip install bless

Windows users must also install the setup API dependency:

.. code-block:: bash

   pip install git+https://github.com/gwangyi/pysetupdi
